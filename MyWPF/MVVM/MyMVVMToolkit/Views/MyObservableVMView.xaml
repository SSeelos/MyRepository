<Window x:Class="MyWPF.MVVM.MyMVVMToolkit.MyObservableVMView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        mc:Ignorable="d"
        Title="MyObservableViewModelView" 
        SizeToContent="WidthAndHeight">
    
    <Border Padding="20">
        <StackPanel>
            <TextBox
                Text="{Binding ViewModel.Name, Mode=Twoway, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="{Binding ViewModel.Name, Mode=OneWay}"/>

            <Button Content="MyCommand"
                    Command="{Binding ViewModel.MyCommand}"/>
        </StackPanel>
    </Border>
</Window>
